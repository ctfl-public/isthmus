@PACKAGE_INIT@

# Load the exported ISTHMUS++ targets so downstream consumers can link the
# library through the namespaced imported target.
include("${CMAKE_CURRENT_LIST_DIR}/isthmus_cppTargets.cmake")
